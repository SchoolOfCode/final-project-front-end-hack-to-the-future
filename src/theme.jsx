import { createTheme } from '@mui/material/styles';

let theme = createTheme({
    palette: {
        primary: {
            main: '#c53',
        },
        secondary: {
            main: '#edf2ff',
        },
    },
});

export default theme;
