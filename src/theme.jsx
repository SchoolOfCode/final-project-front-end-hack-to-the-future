import { createTheme } from '@mui/material/styles';

let theme = createTheme({
    palette: {
        primary: {
            main: '#83C5BE',
        },
        secondary: {
            main: '#edf2ff',
        },
        background: {
            default: '#e4f0e2',
        },
    },
});

export default theme;
